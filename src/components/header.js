import React from "react";

const Header = () => {
  return (
    <header className="h-[60px] w-full fixed top-0 left-0 bg-white flex items-center justify-center">
      手機錢包鑰匙
    </header>
  );
};

export default Header;
